[package]
name = "sandbox-core"
version = "0.1.0"
edition = "2021"

[dependencies]
argon2 = { version = "=0.5.3", features = ["std"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
tempfile = "3"
