// Column-sample contract smoke — lib-internal, placed inside excel.rs tests module
// already exists inline; we just run the typed parse_structure handler path via
// the multipart form by spawning a tiny cargo test inline test.
